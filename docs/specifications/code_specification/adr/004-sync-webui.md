# ADR 004：WebUI 与开放 API 均为同步执行

## 状态

已采纳

## 上下文

提供 WebUI 的产品当前上游是同步接口。异步任务会增加 Jobs 表、轮询与重复计费面。

## 决策

WebUI 表单与开放 API 共用 `Fulfillment` 同步调用。不引入 BullMQ/Kafka 作为执行默认路径。HTTP 超时后失败且不计成功次数。

## 后果

实现与对账简单。长耗时产品可能占满连接，用超时与连接限制兜底；若未来个别产品变异步，仅该产品增加任务模型，不改其余产品。
